---
title: "Container and layout utilities"
description: "Configure container variables and generate layout utilities for Tailwind and Figma."
---

## Container panel

The **Container** panel defines the responsive layout system.

It controls:

- min viewport
- max viewport
- container width
- container margin
- gutter
- mobile columns
- desktop columns
- breakpoint
- overlay color and opacity

These values feed website previews, generated CSS, and Figma variables.

## Exported CSS utilities

The website exports container and column utilities such as:

```text
container-grid
container-grid vw
container-grid fullscreen
container-grid fullscreen vw
container-columns
w-col-*
ml-col-*
mr-col-*
pl-col-*
pr-col-*
gap-col-*
gap-x-col-*
```

Use `container-grid` for normal contained layouts.

Add the `vw` modifier when the section should keep the normal container layout while passing `vw` mode to descendant text-variable utilities.

Add the `fullscreen` modifier when the section should use full viewport width while still sharing system padding, margin, and gutter variables. `fullscreen` is scoped to `container-grid` and does not enable text `vw` scaling by itself.

Use `container-grid fullscreen vw` when a section needs both fullscreen layout and descendant text `vw` scaling.

The generated CSS no longer exports `grid-fluid`, `container-grid-fluid`, or `container-grid-fullscreen` aliases.

## Mobile fallback

When desktop has more columns than mobile, generated CSS includes a fallback so wide `w-col-*` spans become full width below the switch breakpoint unless a `max-switch:w-col-*` override exists.

## Figma import names

The plugin imports container values into `Fluid Settings` with names like:

```text
Container/width
Container/container-width
Container/margin
Container/gutter
Container/column-count
Container/w-col-1
Container/gap-col-1
Container/m-col-1
```

It also creates grid styles named **Container (Light)** and **Container (Dark)** when overlay values are available.

## Related workflow

- Build and restore project settings in [RWA Framework Website](/rwa-framework-website/overview).
- Import the project bundle into [RWA Framework Figma](/rwa-framework-figma/import-and-export).
- Use generated utilities from [RWA Framework CSS](/rwa-framework/rwa-getting-started).
